{
    "name": "demo",
    "version": "1.0.0",
    "description": "",
    "scripts": {
        "clean": "rm -rf .parcel-cache .spago dist node_modules output",
        "build": "spago build",
        "serve": "npm run build && parcel index.html",
        "bundle": "spago bundle-module -m Main -t output/bundled.js && parcel build --no-source-maps index.html"
    },
    "keywords": [],
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "parcel": "^2.0.0-beta.2",
        "purescript": "^0.14.3",
        "purescript-psa": "^0.8.2",
        "spago": "^0.20.3"
    }
}
